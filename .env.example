# Port is managed by the environment (default: 3000)
PORT=3000

# Optional path to python if invoking custom extractor scripts
PYTHON_COMMAND=python

# Optional Gemini API key if AI assistance or analysis extensions are used
GEMINI_API_KEY=
